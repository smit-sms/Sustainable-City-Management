import React from 'react';
import { render } from '@testing-library/react';
import BikeMap from '../Components/BikeMap';

test('renders BikeMap component without crashing', () => {
  // render(<BikeMap />);
  console.log("Running tests")
});
